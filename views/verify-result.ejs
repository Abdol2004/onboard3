<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= success ? 'Email Verified!' : 'Verification Failed' %> - ONBOARD3</title>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Space Grotesk', sans-serif; background: #0a0a0a; color: #fff; min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 2rem; }
        .bg-grid { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background-image: linear-gradient(rgba(57, 255, 20, 0.05) 1px, transparent 1px), linear-gradient(90deg, rgba(57, 255, 20, 0.05) 1px, transparent 1px); background-size: 50px 50px; pointer-events: none; z-index: 0; }
        .container { position: relative; z-index: 1; max-width: 500px; width: 100%; background: rgba(20, 20, 20, 0.8); border: 2px solid <%= success ? '#39FF14' : '#ff4444' %>; border-radius: 20px; padding: 3rem; text-align: center; backdrop-filter: blur(10px); }
        .icon { font-size: 5rem; margin-bottom: 1.5rem; }
        .success { color: #39FF14; }
        .error { color: #ff4444; }
        h1 { font-size: 2rem; margin-bottom: 1rem; color: <%= success ? '#39FF14' : '#ff4444' %>; }
        p { color: #ccc; font-size: 1.1rem; line-height: 1.8; margin-bottom: 2rem; }
        .btn { display: inline-block; background: <%= success ? '#39FF14' : '#ff4444' %>; color: #0a0a0a; padding: 1rem 2.5rem; border-radius: 10px; text-decoration: none; font-weight: 600; font-size: 1.1rem; transition: all 0.3s; margin-top: 1rem; }
        .btn:hover { transform: translateY(-2px); box-shadow: 0 5px 20px <%= success ? 'rgba(57, 255, 20, 0.4)' : 'rgba(255, 68, 68, 0.4)' %>; }
        .logo { color: #39FF14; font-size: 1.5rem; font-weight: 700; margin-bottom: 2rem; }
    </style>
</head>
<body>
    <div class="bg-grid"></div>
    
    <div class="container">
        <div class="logo">ONBOARD3</div>
        
        <% if (success) { %>
            <div class="icon success">✅</div>
            <h1>Email Verified!</h1>
            <p><%= message %></p>
            <% if (typeof username !== 'undefined') { %>
                <p style="color: #39FF14; font-weight: 600;">Welcome, <%= username %>! 🎉</p>
            <% } %>
            <a href="/auth" class="btn">Go to Login</a>
        <% } else { %>
            <div class="icon error">❌</div>
            <h1>Verification Failed</h1>
            <p><%= message %></p>
            <a href="/auth" class="btn">Back to Login</a>
        <% } %>
    </div>

    <script>
        // Auto redirect to login after 5 seconds on success
        <% if (success) { %>
            setTimeout(() => {
                window.location.href = '/auth';
            }, 5000);
        <% } %>
    </script>
</body>
</html>